<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h1 {
            color: #007BFF;
            margin: 20px 0;
        }

        ul {
            list-style: none;
            padding: 0;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        li {
            margin: 10px;
            padding: 15px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: left;
            max-width: 300px;
        }

        li:hover {
            transform: scale(1.05);
            transition: transform 0.3s ease-in-out;
        }
    </style>
</head>
<body>
    <h1>My Movies</h1>
    <ul>
        <% if (dependencies.random) { %>
            <% dependencies.random.movies_info.forEach(movie => { %>
                <li>
                    <h2><%= movie.movie.title %></h2>
                    <p><%= movie.movie.overview %></p>
                    <img src="https://image.tmdb.org/t/p/w200<%= movie.movie.poster_path %>" alt="Movie Poster">
                </li>
            <% }); %>
        <% } else { %>
            <p>Error al recuperar peliculas.</p>
        <% } %>
    </ul>
</body>
</html>
